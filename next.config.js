/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "sainik-updated.vercel.app",
      "resume.zihcc.in",
      "sainik.codekrafters.in"
    ]
  }
}

module.exports = nextConfig